{
    "files.exclude": {
        "**/*.js": { "when": "$(basename).ts" },
        "**/**/*.js": { "when": "$(basename).ts" },
        "**/**/**/*.js": { "when": "$(basename).ts" },
        "**/**/**/**/*.js": { "when": "$(basename).ts" },
        "**/*.map" : { "when": "$(basename).map" }
    }
}